import { styled } from "styled-components";

const Section = styled.div`
    height: 100vh;
    background-color: green;
`;

const Hero = () => {
    return (
    <Section>
        Hero
    </Section>);
};

export default Hero
