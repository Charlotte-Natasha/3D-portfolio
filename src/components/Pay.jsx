import { styled } from "styled-components";

const Section = styled.div`
    height: 100vh;
    background-color: orange;
`;

const Pay = () => {
    return (
    <Section>
        Pay
    </Section>);
};

export default Pay;
